// 카멜 케이스 (cammelCase)
var firstName;

// 스네이크 케이스 (snake_case)
var first_name;

// 파스칼 케이스 (PascalCase)
var FirstName;



// 헝가리언 케이스 (typeHungarianCase)

// type + identifier
var strFirstName;

// DOM 노드
var $elem = document.getElementById('myId');

// RxJS 옵저버블
var ovservable$ = fromEvent(document, 'click');

// 헝가리언 케이스 (typeHungarianCase)